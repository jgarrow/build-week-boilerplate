const initialState = {
    // initialize your state here
};

export const bwReducer = (state = initialState, action) => {
    switch (action.type) {
        // your cases here
        default:
            return state;
    }
};
